import React from 'react'

export default function Hero() {
  return (
    <header className="hero">
      <div className="container">
        <h1>Vivo — Schöne Produkte, die verbinden</h1>
        <p className="lead">Eine kurze, prägnante Aussage warum Nutzer Vivo lieben werden.</p>
        <div className="cta-row">
          <a className="btn primary" href="#">Jetzt starten</a>
          <a className="btn ghost" href="#features">Mehr erfahren</a>
        </div>
      </div>
    </header>
  )
}
